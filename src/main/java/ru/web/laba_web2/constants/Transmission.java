package ru.web.laba_web2.constants;

public enum Transmission {
        MANUAL(0),
    AUTOMATIC(1);
    private int name;

        Transmission(int name) {
            this.name = name;
        }
    }